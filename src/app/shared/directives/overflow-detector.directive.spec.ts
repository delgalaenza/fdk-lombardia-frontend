import { OverflowDetectorDirective } from './overflow-detector.directive';

describe('OverflowDetectorDirective', () => {
  it('should create an instance', () => {
    const directive = new OverflowDetectorDirective();
    expect(directive).toBeTruthy();
  });
});
